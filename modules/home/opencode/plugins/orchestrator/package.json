{
  "name": "orchestrator",
  "version": "0.1.0",
  "private": true,
  "description": "opencode plugin: parallel orchestration (job board, model failover, delegate retry)",
  "type": "module",
  "scripts": {
    "lint": "biome lint .",
    "format": "biome format --write .",
    "check": "biome check --write .",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.2.0",
    "@opencode-ai/plugin": "^1.18.0",
    "@types/bun": "^1.1.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  }
}
